<%= form_for(current_user.collections.find_by(boardgame_id: @boardgame.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Remove", class: "btn btn-large" %>
<% end %>
